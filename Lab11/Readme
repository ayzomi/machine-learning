In this lab, I performed customer segmentation using the K-Means clustering algorithm on credit card customer data. I cleaned and explored the dataset, handled missing values, scaled the features, and used the Elbow Method and Silhouette Score to determine the optimal number of clusters. Finally, I visualized the customer groups using PCA to better understand customer spending behavior and patterns.
